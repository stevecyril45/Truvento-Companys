import { TypewriterTextDirective } from './typewriter-text.directive';

describe('TypewriterTextDirective', () => {
  it('should create an instance', () => {
    const directive = new TypewriterTextDirective();
    expect(directive).toBeTruthy();
  });
});
